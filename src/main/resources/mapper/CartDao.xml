<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rj.dao.CartDao">
    <insert id="add">
        insert into cart(uid,gid,num,money) values (#{uid},#{gid},#{num},#{money})
    </insert>
    <update id="modify">
        update cart set num=#{num},money=#{money} where gid=#{gid}
    </update>
    <delete id="deleteByGid">
        delete from cart where gid=#{gid}
    </delete>
    <select id="findByUid" resultType="com.rj.pojo.Cart">
        select * from cart where uid=#{uid}
    </select>
    <select id="findByGid" resultType="com.rj.pojo.Cart">
        select * from cart where gid=#{gid}
    </select>
</mapper>